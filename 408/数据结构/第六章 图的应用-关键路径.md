正向求最早发生时间取 最大值，是因为要等所有前置任务都完成才能开始（一个没干完，大家都得等）。
反向求最迟发生时间取 最小值，是因为要迁就所有后续任务的底线（一个不能超，整体就不能超）。

### 结论
- **任何一个活动**的持续时间的改变可能会导致**关键路径的改变**。
- 关键路径上活动的时间**延长**多少，整个工期也就**随之延长**多少。
- 延长**公共关键活动**的时间，**不会导致**关键路径的改变。
## 做题技巧
- 若题目要求**关键路径长度**，且图较小，可以**枚举从源点到汇点的各条路径**，分别计算长度。**最长路径长度**即为**关键路径长度**。