- 带权路径长度：两个顶点的路径序列上，所经过的边的权值之和。
- 最短路径：带权路径长度最小的路径称为**最短路径**。（可能**存在多条**。）

## Dijkstra算法求单源最短路径
- 适用的情况：带权有向图，带权无向图。
- **不适用**的情况：含**负权边**的图。

### 算法描述
- 设有**带权有向图**$G=(V,E)$。用一个集合$S$记录**已经找到**最短路径的顶点。
- $visited[i]或finish[i]$：标记顶点$v_i$是否**已找到**最短路径（即是否在集合$S$内）。（最优子结构：某顶点在本轮被选中，其最短路径就确定下来了。）
- $dist[i]$：记录从**源点**$v_s$到顶点$v_i$的**当前的**最短路径长度。（区分：Prim是集合$U$到$v_i$的距离）（“当前”，说明$dist$数组会不断更新。）
- $path[i]$：从源点$v_s$到达顶点$v_i$的**前驱顶点**的编号，可通过**循环回溯**重构出最短路径。

1. 初始化：$visited[] = \{0\}, dist[] = \{\infty\}, path[]=\{-1\}$
2. 设**源点**为$v_s$, 令$dist[s] = 0$，且$dist[j] = g[s][j], visited[s]=1$。
3. 从不在集合$S$中的顶点集合$V-S$中选出顶点$j$，使得$dist[j]$最小。即选择$visited[i]=0$且$dist[j]$**最小**的顶点$j$，将其加入集合$S$，即令$visited[j]=1$。
4. 对于**每个**从顶点$j$出发的**邻接顶点**$k$，更新$dist[k] = min(dist[k], dist[j]+g[j][k])$，若$dist[j]+g[j][k] < dist[k]$，则令$path[k] = j$。
5. 重复步骤3、4，直到所有顶点都包含在集合$S$中，即$visited[k]$均为1。

### 算法时空复杂度
- 未优化＋邻接矩阵，时间复杂度：$O(n^2)$
- **二叉堆优化+邻接表**，时间复杂度：$O((n+e)\log_2n)$，可记为$O(n\log_2n)$

- 空间复杂度：$O(n)$

## Floyd算法求各顶点之间最短路径
- 适用的情况：带权有向图，带权无向图，**带负权边**但**不带负权环**的图。
- 不适用的情况：含**负权环**的图。

### 算法描述
 - 利用了**动态规划**的思想，**迭代**生成一系列$n$阶方阵。
 - $A^{(k)}[i][j]$：从$v_i$到$v_j$，中间顶点**编号不超过**$k$的最短路径长度。
 - $path^{(k)}[i][j]$：从$v_i$到$v_j$所经过的**中转点**的编号（也可叫做前驱结点的编号，同Dijkstra算法），且中间顶点编号不超过$k$，也是**迭代更新**的。和Dijkstra算法一样，最终可通过**循环回溯**，回溯出从$v_i$到$v_j$的路径。

- 其中，$A^{(-1)}[i][j] = g[i][j]$，即原始的**邻接矩阵**。
- 迭代公式：$A^{(k)}[i][j] = min(A^{(k-1)}[i][j], A^{(k-1)}[i][k]+A^{(k-1)}[k][j]), 0\le k \le n-1$，若$A^{(k-1)}[i][j] > A^{(k-1)}[i][k]+A^{(k-1)}[k][j]$，则令$path^{(k)}[i][j]= k$。

- 迭代第$t$次后，$t=1,...,n$，顶点之间的路径的**最大边数**为$t+1$。
- 代码实现：三重循环。

### 手算技巧
- **十字交叉法**：
	- 注意：不考虑对角线元素，即对角线元素为**无效元素**，因为不存在自环。
	- 目的：求$A^{(k)}[i][j]$。
	- 若考虑以$v_k$为**中转点**，则在矩阵$A^{(k-1)}$中，在$v_k$对应的行列的位置进行**十字交叉**，即$A^{(k-1)}[k][k]$（只可能在**对角线**上交叉），形成**垂直对两个轴**。
	- **没被划到的元素**，设为$A^{(k-1)}[i][j]$，**垂直映射**到**两条轴**上，映射的两个位置元素的值相加即为$A^{(k-1)}[i][k]+A^{(k-1)}[k][j]$，和$A^{(k-1)}[i][j]$进行比较，若**小于**，则更新$A^{(k)}[i][j]$；否则**不变**。

### 算法时空复杂度
- 时间复杂度：$O(n^3)$
- 空间复杂度：$O(n^2)$


## 做题步骤规范化总结-Dijkstra算法手算

- 先写表头。
- 假设有$n$个顶点, 则共进行 $n-1$ 轮, 因为起始顶点已经确定最短路径了。
- 但是, 表格中包含了**选中起始顶点的过程**, 保证一致性。
- 所以, 接下来,**有几个顶点就画几行**, 轮次编号从**0到n-1**。

| Step | 已确定顶点集合S         | A                         | B                         | C     | D                        | E     |
| ---- | ---------------- | ------------------------- | ------------------------- | ----- | ------------------------ | ----- |
| 0    | A                | 0 / - <br>(圈起来, 更新距离到下一轮) | ∞ / -                     | ∞ / - | ∞ / -                    | ∞ / - |
| 1    | A, B(填圈起来的顶点)    | 0 / - √（从此以后, 打钩）         | 1 / A <br>(圈起来, 更新距离到下一轮) | ∞ / - | ∞ / -                    | 5 / A |
| 2    | A, B, D(填圈起来的顶点) | 0 / - √                   | 1 / A √<br>（从此以后, 打钩）     | 3 / B | 2 / B<br>(圈起来, 更新距离到下一轮) | 4 / B |
| 3    | 以此类推             |                           |                           |       |                          |       |
| 4    | 以此类推             |                           |                           |       |                          |       |

**填写规则**
- 每个单元格：`最短距离 / 前一跳节点`
- **本轮**确定的节点写在“确定节点”列, 同时**圈起来**。
- 除去本轮被选中已确定节点在距离值后打`√`（例如 `5 / A√`）
- ∞ 用 `∞` 或 `inf` 表示均可