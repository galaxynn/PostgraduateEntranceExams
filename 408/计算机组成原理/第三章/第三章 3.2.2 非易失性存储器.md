- 逻辑上，主存**由RAM和ROM共同组成**，且二者**统一编制**。

## 只读存储器(ROM)的特点
- RAM与ROM均**支持随机访问**，但ROM属于**非易失性存储器**，具有两个显著优势。
	1. 结构简单，**位密度高**于SRAM等可读/写存储器。
	2. **断电后数据不丢失**，可靠性高。

- 根据制造工艺和可编程性，ROM可分为以下几类。
	1. 掩模式ROM（MROM）：由厂商固化，用户不可更改。
	2. 一次可编程ROM（PROM）
	3. 可擦除可编程ROM（EPROM）