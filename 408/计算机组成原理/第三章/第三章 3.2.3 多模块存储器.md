- 多模块存储器是一种空间并行技术，通过将多个**结构完全相同的存储模块**并行工作来提高存储器的吞吐率。
- 根据**不同的编址方式**，多模块存储器分为**连续编址**和**交叉编址**两种结构。

## 连续编址方式
- 在连续编址的多模块主存储器中，主存地址的**高位表示模块号(或体号)**，**低位表示模块内地址(或体内地址)**,
- 因此，也称为**按高位地址划分方式**，**地址在模块内连续**。