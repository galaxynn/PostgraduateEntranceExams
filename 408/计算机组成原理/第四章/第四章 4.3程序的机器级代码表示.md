- MIPS 指令通常会在试题中**附带功能说明**，而x86 指令则更常作为**默认考查对象**。因此，本节**重点介绍 x86 汇编指令**。

- 值得注意的是，**在x86指令集中**，**寻址方式**被**显式的包含**在**地址码字段**中。（⭐）
## x86相关处理器
- 按**字节**编址。
- **x86 处理器**中有**8个32位通用寄存器**，主要寄存器及说明如图所示。
- ![[第四章 程序的机器级代码表示 寄存器.png]]
- 为向后兼容早期的16位和8位架构，**EAX、EBX、ECX和EDX**的**低16位**可**作为独立寄存器使用**(分别记为**AX、BX、CX、DX**)，其中，E表示**Extended**，用于标识32位寄存器。
	- **每个16位寄存器**又可**进一步拆分为两个8位寄存器**
		- 例如：**AX分为AH和AL，BX分为BH和BL，以此类推**，H表示High，即高8位；L表示Low，即低8位。
- 除EBP(基址指针)和ESP(栈顶指针)外，其余通用寄存器的用途是比较灵活的。
- ![[第四章 程序的机器级代码表示 8086.png]]

## 常用汇编指令介绍
- 汇编指令通常可分为**数据传送指令**、**算术与逻辑运算指令**和**控制流指令**。

- 以下**用于操作数的标记**分别表示寄存器、内存和常数。
- `<reg>`：表示**任意寄存器**，若其后**带有数字，则指定其位数**。
	- `<reg32>`表示32位寄存器(eax,ebx, ecx, edx, esi, edi, esp或ebp);
	- `<reg16>`表示16位寄存器(ax, bx, cx或dx);
	- `<reg8>`表示8位寄存器(ah, al, bh, bl, ch,cl,dh, dI)，
- `<mem>`：表示**内存地址**(如[eax]、[var+4]或dwordptr[eax+ebx])。
- `<con>`：表示8位、16位或32位常数。
	- `<con8>`表示8位常数；
	- `<con16>`表示16位常数；
	- `<con32>`表示32位常数。

- x86 指令采用**变长编码**，其**操作码通常为1字节**，但**整条指令长度可变**。
	- 同一指令(如mov) 因**操作数类型或寄存器不同**，可能对应**多种机器码编码**，例如：
	- ![[第四章 程序的机器级代码表示 mov1.png]]

- `dword/word/byte ptr + [A]`：指明内存读写长度为**双字**/**单字**/**单字节**。
- [A]：表示取**寄存器或地址A**中的**内容**。类似于C语言中，**指针解引用**。

### 数据传送指令
#### 1. mov指令
- 格式：`mov dest, src ;dest <- src`
	- **操作数 = 数据本身 或 数据的存放地点**
	- **源操作数**可以是**立即数、寄存器或主存单元**，**目的操作数**可以是**寄存器或主存单元**，但**不能是立即数**。
- 作用：把1字节或**字**（需要显式表示）的**操作数**从**源地址 src** 传送至**目的地址 dest**。
```
mov al, 4; al <- 4，将立即数的值传送到al寄存器（立即数寻址）
mov byte ptr [2000h], 5 ; 将常数5存入地址 2000h 处的1字节内存单元（直接寻址），byte ptr 说明是字节操作
```
- **双操作数指令**的**两个操作数不能同时为内存**，即 mov 指令不能用于直接从内存复制到内存。 
	- 若**需在内存之间复制**，可先将**源内存内容加载到寄存器**，再**从该寄存器写入目标内存**。（利用**寄存器中转**。）

#### 2. push指令
- 格式：`push <reg32>/<mem>/<con32>`
- 作用：**将操作数压入栈中**，常用于**函数调用和现场保护**。
- 栈中元素**固定为32位**，即**4字节**。
- ESP是**栈顶指针寄存器**，入栈前**先将ESP减4**(栈向**低地址方向**增长)，再将操作数**压入**ESP所指地址。（先减，再压。）
	- 不变量：ESP指针**始终指向当前栈顶元素**。
```
push eax ; 将 eax 的值压栈
push [1000h] ; 将 地址1000h 处的 4字节内容 压入栈中
```

#### 3. pop指令
- 格式：`pop <reg32>/<mem>`
- 作用：与push指令相反，从栈中**弹出栈顶**，并将其**保存到指定位置**。
- 出栈前先将ESP所指地址的内容**读出**，再将**ESP加4**。（先读，再加。）
	- 不变量：ESP指针**始终指向当前栈顶元素**。

### 算术运算和逻辑运算指令
#### 1. add/sub指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 addsub.png]]
- 作用：add 指令将两个操作数**相加**，sub 指令将第一个操作数**减去**第二个操作数，结果**均保存在第一个操作数(目的操作数)存放的位置**。
- 值得注意的是，第一个操作数**不能为常数**，两个操作**数不能同时为内存。**
- ![[第四章 4.3程序的机器级代码表示 addsub-1.png]]

#### 2. inc/dec指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 incdec.png]]
- 作用：分别对操作数执行**自增1**或**自减1**操作。
- ![[第四章 4.3程序的机器级代码表示 incdec1.png]]


#### 3. imul/mul指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 imul.png]]
- 作用：**imul**指令为**有符号整数**乘法指令，支持两种格式：
	- ① **双操作数**，将第一、第二操作数相乘，结果存入**第一个操作数**(必须为**寄存器**);
	- ② **三操作数**，将第二、第三操作数相乘， 结果存入**第一个操作数**(必须为**寄存器**)。
- ![[第四章 4.3程序的机器级代码表示 mul.png]]

- **无符号整数乘法**由 **mul 指令**实现，仅支持**单操作数格式**，被乘数**隐含在eax中**，乘积结果存放在**edx:eax中**。（同**乘法器**）
	- 格式：`mul <reg>/<mem>; `

- imul在结果溢出时，即edx**不是**eax的**符号扩展**时，置 **CF=1 和 OF = 1**。
- mul在结果溢出时，即edx≠0时，同样置**CF=1和OF = 1**。
	- **无符号乘法**以**CF**判断溢出，**有符号乘法**则以 **OF** 为准。

#### 4. idiv/div指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 idiv.png]]
- 作用：**有符号整数**除法指令，仅指定**除数**。
	- **被除数**为 edx:eax 组成的**64位有符号数** (edx 为高 32位,eax为低32位)。执行后，**商存入eax**，**余数存入 edx**。（**余数在高位，商在低位**，同**除法器**）
- ![[第四章 4.3程序的机器级代码表示 idiv-1.png]]

- **无符号整数除法指令 div** 的格式与 idiv 的**完全一致**，仅对操作数的**解释不同**。

#### 5. and/or/xor指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 andorxor.png]]
- 作用：分别执行**按位与、或、异或操作**，结果存入**第一个操作数（目的操作数）存放的位置**。
- ![[第四章 4.3程序的机器级代码表示 andxor.png]]
- 小技巧：
	- ① 只**保留二进制中的某几位**时，可以**利用and**。
		- 实例：**子网掩码**。
	- ② **偶数**个1相异或，结果为0；**奇数**个1相异或，结果为1。

#### 6. not 指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 not.png]]
- 作用：**按位取反**指令，将操作数的每一位**0变1、1变0**。
- ![[第四章 4.3程序的机器级代码表示 not-1.png]]

#### 7. neg 指令
- 格式：
	- ![[第四章 4.3程序的机器级代码表示 neg.png]]
- 作用：**取负指令**，计算操作数的二进制补码(-x)，即给操作数执行**取反加一**的操作。
- ![[第四章 4.3程序的机器级代码表示 neg-1.png]]