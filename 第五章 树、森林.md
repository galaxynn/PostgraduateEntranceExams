
## 树的存储结构（一般的树）
- 树的存储结构有多种，既可以采用顺序存储结构，又可以采用链式存储结构。无论才有哪种结构，都必须能够唯一地反映树中各结点之间的逻辑关系。

### 1. 双亲表示法
> 双亲，即父节点。
- 本质：**结构体类型数组**。结构体中存的是ElemType data, int parent；
- 定义：双亲表示法使用一组连续的存储空间来存放树中的所有结点。
- data是数据域，parent是**伪指针域**，表示其**双亲结点在数组中的下标**。

- 双亲表示法利用了每个结点（除根节点外）有且仅有一个双亲性质。
- 优点：**查找**任意结点的**双亲**非常高效。（应用：**并查集**。）
- 缺点：查找孩子结点需要遍历**整个数组**，效率低下。

### 2. 孩子表示法
> 和邻接表原理一致。顺序 ＋ 链式。
- 定义：孩子表示法将每个结点的**所有孩子**视为一个**线性表**，并用**单链表**存储。且所有结点的**头指针**被集中存在在一个顺序表（**数组**）中，每个位置代表一个结点。
- 优点：查找孩子结点效率较高。
- 缺点：查找双亲结点需要遍历**所有链表**，效率低下。

### 3. 孩子兄弟表示法
- 定义：**孩子兄弟表示法**也称**二叉树表示法**，采用二叉链表作为树的存储结构。
- 每个结点包含三部分：数据域、指向**第一个孩子**的指针，指向**下一个兄弟**的指针。（左(指针)孩子右(指针)兄弟。）这种结构，将树**映射为二叉树形式**。
- 优点：灵活性好，能**自然地将树转化为二叉树**，而**复用二叉树的算法**。**查找孩子**非常高效。
- 缺点：查找双亲结点困难。（优化：增设parent域，指向父节点，构成三叉链表。）

- **物理结构**上看，二叉树的二叉链表表示法与树的孩子兄弟表示法**完全相同**。
- 本质上，孩子兄弟表示法和二叉链表表示法是**同一物理结构的不同解释**。

## 树、森林、二叉树的转换
### 转换的性质
1. 转化前后，**结点总数保持不变**。（⭐⭐）
2. 若为**树和二叉树**相互转换，**树中有 $n$ 个非叶结点，则对应二叉树中有 $n + 1$ 个空右指针域**（结点）。（树中非叶结点$A$必有孩子结点，在对应二叉树中，该非叶结点$A$**必有左指针指向第一个孩子结点**，该孩子结点右指针依次链起其所有孩子结点，**最后一个孩子结点的右指针域必为空**。）（加1是因为**树转化而来的二叉树根节点右指针为空**（无兄弟）。）（⭐⭐⭐）
3. 树中的**叶结点**，对应二叉树中**左指针为空**的结点。（叶结点无孩子，对应二叉树中该结点的左指针**必为空**。）（⭐⭐⭐）
4. 若为**树和二叉树**相互转换，转化前后，**总边数保持不变**。
5. 若为**森林**转换为**二叉树**，转化前后，总边数**增加**，增加条数 $k = 森林中子树棵数 - 1$。（减1是因为其中一棵子树的根要作为二叉树的根结点。）若为**二叉树**转化为**森林**，总边数**减少**，减少条数与增加条数相同。
6. 二叉树转化为树或森林的结果是**唯一**的。
### 1.树转化为相应的二叉树
- **依据左指针指向第一个孩子，右指针指向兄弟的定义**即可轻易转化。

### 2.森林转化为二叉树
- **依据左指针指向第一个孩子，右指针指向兄弟的定义**即可轻易转化。
- 森林中含多颗树，先将**每棵树先转换为对应的二叉树**。
- **将每棵树的根节点视为兄弟**，由于转化后的二叉树的根结点的右指针本身为空，可用右指针依次将多棵转化成的二叉树串起来，最终构成一棵完整的二叉树。

### 3. 二叉树转化为森林
- **依据左指针指向第一个孩子，右指针指向兄弟的定义**即可轻易转化。
- 以上过程的逆过程，先将各右指针断开，分离出各棵二叉树。最后将各棵二叉树依据定义，转化回普通树。

## 树和森林的遍历
- 记忆：**先对先，$X$对中。**$X$要么为树的后根遍历，要么为森林的中序遍历。反正除先序外的，对应中序。
- 题目可能会给出树/森林的遍历序列，我们要把它对应到二叉树的相应遍历序列，从而可以构造出二叉树。
### 树的遍历
- **树的先根遍历**：先访问根节点，再依次先根遍历根节点的各子树。与这棵树**相应的二叉树**的**先序遍历**序列相同。
- **树的后根遍历**：先依次后根遍历根节点的各子树，再访问根节点。与这棵树**相应的二叉树**的**中序遍历**序列相同。
### 森林的遍历
- **森林的先序遍历**：①访问第一棵树的根节点，②依次**先序遍历**第一棵树根节点的各子树，③先序遍历除去第一颗树后的**剩余森林**。与这个森林**相应的二叉树**的**先序遍历**序列相同。
- **森林的中序遍历**(有些教材也称为**森林的后序遍历**)：①依次**中序遍历**第一棵树根节点的各子树，②访问第一颗树的根节点，③中序遍历除去第一颗树后的**剩余森林**。与这个森林**相应的二叉树**的**中序遍历**序列相同。